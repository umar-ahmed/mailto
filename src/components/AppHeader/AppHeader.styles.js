import styled from "styled-components";

import { Typography } from "@material-ui/core";

export const ExpandedTitle = styled(Typography).attrs({
  variant: "h6",
  color: "inherit"
})`
  flex-grow: 1;
`;
